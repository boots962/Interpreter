This is a simple lexer-parser interpreter. At the moment it can only do simple command line arithmetic while storing variables in a data structure. 
In future commits it will read files and interpret the programs. -- Will update in May 
